name: CI/CD Kubernetes Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build backend
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/photo-backend:v1 ./backend
          docker push ${{ secrets.DOCKER_USERNAME }}/photo-backend:v1

      - name: Build frontend
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/photo-frontend:v1 ./frontend
          docker push ${{ secrets.DOCKER_USERNAME }}/photo-frontend:v1

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~/final-project
            git pull origin main
            kubectl apply -f k8s/
            kubectl rollout restart deployment backend
            kubectl rollout restart deployment frontend
            kubectl rollout restart deployment mongodb
